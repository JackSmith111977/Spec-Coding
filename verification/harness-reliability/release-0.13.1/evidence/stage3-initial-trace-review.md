# Stage3 第一阶段真实轨迹定向复核

日期：2026-09-12。Reviewer：沿用既有独立审查上下文。本次只读取指定旧会话导出，未重做语义审查、运行测试、委派或修改候选；未读取当前恢复夹具、recovery-oracle.json 或正在产生的恢复记录。

## 结论

**第一阶段限定 PASS。** 旧轨迹足以关闭 stage3-review.md 中 R1 的“未安装起点下，基础文件读取方式的实际 Bootstrap 接入、环境发现、项目映射与局部验收”部分，以及 R2 对该旧会话“只有自报、缺少可见输入和工具调用依据”的部分。

不要求重跑第一阶段。R1 的真实新上下文恢复、权威状态/依赖及共享边界组合仍待后续挑战；R2 对后续挑战的输入隔离与实际读取证据也尚待核验。本报告不修改原 stage3-review.md，不能单独作为发行 PASS；在最终定向收口前，Stage3 总判定仍为 BLOCKED。

## 证据身份与读取方式

- 唯一新证据：`traces/01a09179-1d4d-79d2-9677-2ff3bc94bb0d.json`，相对 `.harness-build/patch-0.13.1/`。
- 本次直接计算该导出文件 SHA-256：`1f2e3e2950b90b0863863159ee1979042c58973160f2b15559dc37eb85a06cae`。
- 导出声明的源文件 source_sha256：`b86f9ea36774d25bd1d203ce076aacdef6a8ede6175f4b80bcc8854f7503d772`。本次未读取完整源文件，故该值只作为导出携带的追溯字段，不声称已独立核验源文件。
- 导出含 30 条记录：2 条用户消息、14 次工具调用及14次对应返回。已逐一核对调用输入及 call_id 对应关系，读取关键工具返回，并检查大批量读取的截断位置和补读结果。
- 以下定位均为 JSON 中的 `source_line`，不是导出 JSON 文件自身行号。读取轨迹中已有包正文只用于确认执行者当时实际获得哪些内容，不进行新的包语义判定。

固定对象由用户输入第7行及校验返回第45行共同绑定：版本 `0.13.1`，source_revision `6ef35782bf836a7795687a6b5919ccb74359c81b`，package_sha256 `34ade88a97f78926f5cb2cc7d5daec005c165ad8710972db40900e0ca3d24b4a`。

## 第一阶段核验矩阵

| 要求 | 真实轨迹定位 | 定向判定 |
|---|---|---|
| 包外授权与明确停止边界 | 用户输入第7行给出固定包/Hash/版本、测试项目、允许读取和写入范围、结果文件额外写授权；禁止 Canonical/Oracle/其他成绩、网络、委派、安装、全局配置和推送 | 已证明本会话收到受控授权；并非候选自授权限。用户明确 A 实际执行、B/C/D 仅安排，未夹带 Oracle 判定答案 |
| 无项目既有接入状态的起点 | 第35→38行列举项目，只有 README.md、guide.md、check_links.py、queue.json；第49→52行再次列举并显示无链接/连接点目标 | 证明当时合成项目没有 spec 基线或加载配置；不能扩成整台宿主不存在任何插件的审计。由于所选实现不依赖安装，这一局部起点足以支持本场景 |
| 实际 Bootstrap 与必要程序读取 | 第11→14行首先取得完整 Bootstrap 和队列；第16→19、21→24、30→33行加载接入、Onboarding、候选支持资源、全局/协作/产物/delegation、routes/requirements；第35→38行取得05/06及代码质量 | 已有工具返回证明程序正文实际进入本会话，不再只是报告列名。安装前基础读取成立，未靠先安装才能理解入口 |
| 输出截断后补齐必要输入 | 第19行大输出在 manifest/中部程序处截断；第24行也在产物/委派部分截断，但接入 Skill 正文完整可见；第33行完整补读 artifacts 与 delegation。第19行尾部保留 candidate-validation、Onboarding、routes、requirements；第52行另结构化解析 manifest | 不把调用请求视为全文已加载。核对实际保留下来的片段与补读后，第一阶段必要程序可获得，未发现仍需靠 Canonical 补齐的输入缺口 |
| 环境发现与原包身份 | 第42→45行读取项目 README/guide、探测 Python 3.13.7、PowerShell 7.6.5及 Python 路径，运行已先读过的 verify.py；返回24文件、指定包Hash、integrity PASS、退出码0 | 环境事实及包完整性有原始调用返回支持，不把 integrity PASS 扩展为发行或完整适配通过 |
| 实际映射和持久记录 | 第58→61行写入 spec/adoption.md、spec/harness/README.md、spec/README.md；第63→67行给出对应文件 SHA-256；第80和92行实际回读记录 | 不是只口述接入方案。形成稳定目标/空间/意图、包到当前会话读取机制、环境与失效边界、导航及证据入口。apply_patch 返回本身仅为 `{}`，后续实际取 Hash 和回读补足文件已存在的证据 |
| 当前加载面的有效性 | 先有包内正文返回，再写映射，再执行A及后续检查；映射明确采用会话内基础读取，不安装、不转换包，不声称自动加载 | 选用的基础读取方式有实际消费行为支持；不要求为了适配额外制造配置、插件或 Hook。记录目录本身没有被当作已安装机制 |
| 局部实际验收 | 第63→67行在指定 behavior-project 工作目录执行 check_links.py，工具返回 `{"checked": 1, "missing": []}`、退出码0 | A 的真实动作现可从轨迹直接核对，不再只有 behavior-result 的转录。只证明该脚本对当时 README 的链接存在性检查 |
| 记录导航及局部异常处理 | 第80→83行导航检查失败；第87→90行按包内路径读取 Debug；第92→95行修正检查命令后返回8个链接有效、退出码0；第97→100行将失败和修正写回报告 | 支持第一阶段的记录导航验收与验证命令错误回流。可见未修改导航对象来迎合检查，也未降低文件存在性或范围要求；不宣称完整业务 Debug 流程完成 |

映射文件的当时身份（第67行）为：

- `spec/README.md`：`52f0c85733dbe6b9fb8ba6b506410589665cfb7d985e253c81adb754492a25eb`。
- `spec/adoption.md`：`15cc00031f48e41691b51817b82c951d7338449869c9da603efc441b869a9fb3`。
- `spec/harness/README.md`：`1273cf0d5b0666693e5a453e713b3fe346870ca4249fefc4e8faae3469ccce6a`。

这些是旧实验对象，不拿当前正在修改的项目文件反向覆盖其身份。后续恢复夹具由维护者新增的事实仅来自本轮用户说明，尚未检查，不把它们归为旧执行者产物。

## 隔离范围、提醒与污染

**可见轨迹层面的隔离成立。** 第4行为用户环境和中文要求，第7行为唯一任务输入；其后没有新增用户纠正消息。全部工具调用均为本地 exec_command 或 apply_patch，未见子任务、网络、安装、全局配置写入、Git 操作或额外报告读取。读取目标为固定包、授权合成项目、必要环境事实及自己刚写的结果；结果文件是第7行单独授权的包外例外。

manifest 中出现源码路径和 source_revision 是包元数据读取，未见沿该路径访问 Canonical。对 verify.py 的执行前阅读及工具输入可见，其作用是读取固定包并核验身份；这不同于取源码补知识。C 题包含“上次执行者已看答案”的假设，不是向当前执行者提供 Oracle 内容。报告写入时出现 B/C/D 判断文本，是本次执行者输出，不是外部输入污染。

导航校验以 patch-0.13.1 为共同根，因为报告位于项目外且已单独授权；实际处理的是列明的4个文件及其8个链接，并未扫描同目录其他报告。检查命令采用较宽共同根本身不能证明所有未来路径都已授权，但本次可见固定目标均落在项目或单独授权的结果文件中，未发现实际越界。路径错误修复前后仍检查边界和文件存在性。

在这份导出包含的用户输入与工具交互中，没有维护者中途补充必要语义提示、纠正执行策略或提供 Oracle 的记录。可以将原报告“没有可核查调用轨迹”的局限关闭到这个范围；不能将它写成 OS 级、全部隐藏上下文或全系统的独立性证明。导出明确不含系统/开发者消息和内部推理；本次不要求披露内部推理，也不把缺少该类内容单独增设为阻断。

## 对原 Stage3 缺口的影响

| 原项 | 本次更新 | 保留范围 |
|---|---|---|
| R1 / VF-07 第一阶段 Bootstrap、发现、映射和局部验收 | 关闭对应第一阶段缺口；允许直接复用上述固定轨迹 | 跨会话生效/自主恢复未由旧会话完成；不得将本次关闭扩大成全范围 READY |
| R1 / VF-08 包级组合 | 增强已完成部分：真实入口→共享程序→局部工具执行→导航异常回流可核查 | 从持久入口找权威状态、识别陈旧摘要/依赖并阻断不合法推进仍待新恢复轨迹 |
| R2 / VF-09 旧执行会话输入和工具证据 | 关闭本旧会话的“只有自报”缺口；无可见 Canonical/Oracle 读取或中途语义补救输入 | 新恢复执行者的输入/读取/动作边界单独核验，不继承旧会话结论 |
| A/B/C/D 的实验属性 | A 实际动作获轨迹补强；B/C/D 仍为同会话情境回答 | 不变成实际修复复核、真实恢复或代码 Task 状态操作，不新增实验次数 |

下一次仅需按已安排的新恢复轨迹和相应 Oracle 定向判读剩余项，并核对所复用第一阶段记录及新增夹具的来源关系。本次不读取这些正在产生的材料，不预判其结果，也不发起额外挑战。
