# 0.13.1 Stage3 最终定向收口

日期：2026-09-12。Reviewer：本轮原独立语义 Reviewer，沿用原审查上下文。本次只核验剩余 R1/R2，不重复全量语义审查、不运行新测试、不委派、不修改冻结包，仅写本报告。

## 最终判定：PASS

治理 §3.4–§3.6 对本次增量构建要求的必要覆盖已满足：原结构收据通过，独立语义无 Blocking Finding，既有有限行为挑战及补充的真实接入/恢复包级挑战有对应证据，验证对象保持同一固定候选。**R1、R2 已关闭，无剩余 Stage3 发布阻断项。**

本结论取代 stage3-review.md 的待补证 BLOCKED，历史报告和失败记录保持原样。通过的是本轮受影响范围及代表性包级集成，不是全 Runtime、完整业务闭环或所有 Harness 能力认证。合成项目中的 T1/E1 未解决、T2 未完成，正是负向边界挑战应保留的结果，不构成实验失败。

## 固定对象和证据来源

- 候选：`.harness-build/patch-0.13.1/frozen-r1`，版本 `0.13.1`。
- source_revision：`6ef35782bf836a7795687a6b5919ccb74359c81b`。
- package_sha256：`34ade88a97f78926f5cb2cc7d5daec005c165ad8710972db40900e0ca3d24b4a`。
- 原基线 package_sha256：`3a32530d2d28dd43b8c0afed75faa13c130a04d39b78cfce6afa7ed81906540c`。不以旧包整体 PASS 代替本候选判定。
- 复用：build-r1/archive-r1 全包结构收据、semantic-review、behavior-verdict、stage3-initial-trace-review。首次接入证据已经核验，无需重跑；其对象、包内来源、基础读取方法和原身份保持有效。
- 新增：事先 `recovery-oracle.json`、`traces/01a091c2-2fa5-7c40-8fa8-f39dd937aa40.json`，以及 `behavior-project/spec/recovery/{result,evidence,writeback,tasks,summary}.md`。

按用户补充，以重新导出的当前轨迹为准，本次再次读取并计算摘要；导出仍为29条可见记录，3条用户消息、13次工具调用及13次匹配返回：

| 对象 | 本次核验的 SHA-256 |
|---|---|
| 当前恢复轨迹导出 | `7d484d6dcc4e8b39e03115bd5032a11ce5235db1c2ae4b3170f89ca8c262a1d6` |
| recovery-oracle.json | `1f16eb207d25e3242818bbb71a57d650499b7bbbca9e5e93d7d68786ee81ce53` |

导出携带的 source_sha256 为 `d6a7a2847702897103b69c60ad7fef711a8a0eabd6c25f5690d14a809f0729ee`，它是源日志追溯字段，本次未读完整源日志，不能等同于独立核验该源文件。用户说明收口消息后还有一次简短最终答复并已关闭会话；这类答复不在所给用户/工具导出内，本判定不依赖它，也不另算实验轮次。

## 新恢复轨迹：直接观察与判读

以下行号均为轨迹中的 source_line，不是导出文件的物理行号。

| 事先 Oracle 要求 | 可核查证据 | 判定 |
|---|---|---|
| 从项目入口定位绑定与权威状态 | 第7行给项目入口和包外授权；第11→14行首先读取 spec/README；第16→19行读取原 adoption、harness 绑定及恢复摘要；第21→24行实际取得 tasks 和 Bootstrap | PASS。没有把旧会话的“已加载”当当前加载；由持久入口找到固定包及新夹具 |
| 不把陈旧摘要当可执行证明 | 第19行摘要称“可以继续完成收口”；第24行原权威正文明确 T1 Blocked、T2 Pending DependsOn T1、E1 未确认 | PASS。之后的实际写回保留阻塞，未顺从摘要宣称完成 |
| 只消费包及场景，沿共享路由加载 | 第26/35行读取共享规则、接入/Onboarding、routes/requirements；第45→48行补读接入和 delegation；第52→55行沿路由读取06、02及校验程序；第71→74行读取代码质量 | PASS。核对了返回中的截断和后续补读；不能只凭调用清单宣称全文已读，但本场景必要共享程序已可获得 |
| 固定对象并真实执行局部检查 | 第59→62行 verify.py 返回24文件、指定包Hash、integrity PASS；第64→67行先读取 README/检查脚本；第78→81行实际执行 `python ./check_links.py`，输出 `{"checked": 1, "missing": []}`、退出码0 | PASS。不是只在报告里抄写一个预期结果；局部检查含义仍限 README 的一个文件链接 |
| 不越过依赖或权限完成 T2 | 第87→90行实际 patch 保留 T1/E1、Owner和 DependsOn，T2 写为 Blocked，记录需求Owner后续范围确认与验证Owner承接点 | PASS。没有假 Done、假正式 PASS 或代需求Owner接受范围 |
| 允许纠正导航并保留历史 | 第78行先保存原 tasks/summary 和指纹；第87行再更新权威表并把 summary 改为静态权威/证据导航；第94→97行实际回读自检，11链接、0缺失、状态与入口 True | PASS。先证据、后权威、再导航，有实际写入和读取返回，不仅是自报 |

工具执行顺序与结果文件相符：evidence 中的局部输出由第78行命令直接捕获；包校验段在第94行补录此前第62行的真实结果，不是第二次运行。writeback 是执行者的自检，不独自构成独立通过；本 Reviewer 已对照其命令、返回及当前产物正文判断。

本次对当前四个输出文件直接计算 Hash，与 writeback 中记录一致：

| 文件 | SHA-256 |
|---|---|
| tasks.md | `89ba90b372565af5cab5046dc2d7da187c467c5455dcf80256169c78fd96430d` |
| summary.md | `d39c921f57366b365ff91802f82e7785ed012c655eb1af0946f6e860df40995c` |
| result.md | `35a49b752323b9e9f731815eb0d43cb3392dd787a896ab48270fbecf1d2c281a` |
| evidence.md | `90202266a75f6b4fa7bacb4f3fc437451abf5621046b7136b1b4aa14c2e0f58f` |

writeback.md 本次摘要为 `1c2526d2b7f3c547e89124e724611337a9adcbf7e72b661d23f1a11fd6f1cb53`。文件 Hash 核验只用于证据身份，没有运行新行为测试。

## 夹具、状态及隔离的边界判断

**夹具来源明确。** 初始 T1/T2、故意陈旧 summary 及项目入口扩展由主 Agent 准备，属于事先场景，不是执行者自主创建的业务产物。新执行者实际产生的是 evidence、result、writeback 和 tasks/summary 的后续变更。恢复 evidence 中 adoption 与 harness/README 的 Hash 与旧第一阶段相同，项目总入口 Hash 已变为 `9dbbc63f82d985e11e2b3fd748381c414cce86b4724958b62d3a2159f06fc182`，与新增恢复导航相符；不把新增夹具冒充旧实验的未变对象。

**T2 的 Blocked 不等于实验失败。** 事先 Oracle 要求不越过 T1、保留 Owner 承接点，并未要求本实验消除 E1。这里是使用 Pending 的合成验收任务表，T2 内容是整体验收收口；将不能收口写为 Blocked，在本场景中记录了真实验证阻塞。它不是05正式开发 Task Graph 的 Ready 调度实验，不能据此推广“上游 Blocked 就应级联修改所有正式下游状态”；05 的原 Ready/不可运行区分仍保持原要求。本次不认证该正式状态转换路径。

**读取隔离有可见证据。** 已核对全部可见调用：没有读取 Canonical、Oracle、旧 behavior-result、其他审查报告或宿主日志；没有网络、委派、全局安装、Git 写入或冻结包修改。原 harness 绑定含旧结果链接和旧范围说明，这是授权的持久接入记录；执行者没有沿链接读取旧报告。第52行读取旧 queue.json 是入口可达的题目契约，不含执行答案，当前用户也未禁止读取项目题目文件；不把它说成零历史文本输入，也不把它误判为读取旧成绩。包 manifest 的源码路径/修订字符串及校验时的原始字节读取，不等于读 Canonical 或使用未加载正文补知识。

**有预算消息，不是零消息。** 第106行要求已有必要动作检查完成后收口、不扩展测试、按真实阻塞交付；其位置在第97行写回检查成功之后。它没有指出该读哪个规则、该如何改状态或给出 Oracle 答案，故不属于挽救必要语义行为的提示。可见状态判断、检查和写回在此之前已完成，不以该消息证明或否定首次恢复行为。

本结论建立在用户提供的受限真实可见导出上，不是 OS 审计或系统/开发者消息审计，也不声称有不可见信息的全量独立性证明。无需披露内部推理。首次接入与新恢复是两个实际执行上下文，但不是两次相同实验的独立重复，也不是无输入、无场景前提的业务自主性认证。

## 剩余 R1/R2 与最终覆盖矩阵

| validation_focus / 治理条件 | 最终证据与处置 |
|---|---|
| VF-01 全包结构和固定身份 | 原 build-r1/archive-r1 结构收据、语义阶段独立身份核验及两次行为中的包校验均绑定 frozen-r1；通过 |
| VF-02 affected_artifacts 独立语义及共享组合 | semantic-review 原独立直读结论继续有效，无 Blocking Finding；不重做全量审查 |
| VF-03 确定性优先 | A 实际命令及旧轨迹；新恢复再次实际执行局部工具且不扩大结果含义；覆盖 |
| VF-04 原 Reviewer 复核、证据复用决策 | B 情境判断及原判读保留；不是实际修复实验。首次接入按原对象与方法复用，新夹具另取证；覆盖本轮所需决策焦点 |
| VF-05 必要 Fresh | C 的隔离选择决策，加本次独立新会话从持久入口实际恢复；覆盖所需恢复片段，不扩成全类型恢复认证 |
| VF-06 权限/Gate/代码对象边界 | D 的负向情境判读继续有效；新恢复实际保留 E1/T1 阻塞、拒绝整体收口；不声称真实 Git 生命周期完成 |
| VF-07 真实接入与映射 | stage3-initial-trace-review 已关闭首次接入部分；新轨迹重新加载相同绑定和必要资产，未借旧已加载声明；R1 对应缺口关闭 |
| VF-08 共享规则和入口转换包级集成 | 真实项目入口→旧稳定绑定→固定Bootstrap→共享程序→新权威状态→局部验证→阻塞与导航写回，加旧接入导航异常回流；R1 剩余缺口关闭 |
| VF-09 可核查输入、隔离、工具和写回 | 两份可见轨迹、匹配调用返回、预写回状态及当前输出身份；预算消息单列，不将自报升级为全程隐藏上下文认证；R2 关闭 |

有限增量覆盖足以满足本次发行的理由是：四份变更来源及所有受影响组合已有独立语义覆盖；原本不能由问答代替的真实接入/新上下文恢复现已执行；normal、boundary、authority/gate、异常/路由及共享消费变化均有与其风险相称的代表性证据。治理不要求每个 Skill 重跑、制造实际修复、穷举 Runtime 或完整产品闭环。本次没有降低通过条件，也没有用范围声明掩盖仍然适用的必要缺口。

## 发行交接与保留限制

本固定候选可按治理进入 Stage4 发布身份收敛。Stage4 仍需核对实际发布包/归档与已验证候选一致；本报告不是“已经推送/发布”的事实，也不代替主 Agent 的附件脱敏和发行操作。

发布说明必须保留：旧阶段曾因证据不足 BLOCKED、第一阶段导航检查失败及修正、A 实际/B-C-D 情境的区别、真实恢复的合成性质与范围、末尾预算消息，以及 T1/E1 未完成、T2 业务阻塞。不得宣称全业务闭环、全 Runtime 兼容、自动安装/持久加载或所有隔离机制已经认证。

只要候选身份不变，本次补证无需重建包；任何候选内容变更仍按治理重新固定并确定受影响验证。本轮 Stage3 最终结论为 **PASS**。
