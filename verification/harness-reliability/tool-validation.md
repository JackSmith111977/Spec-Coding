# 未提交变更验证

日期：2026-09-12。结论：本轮确定性工具验证通过；不代表新Harness发行、客户端行为或整套改进计划已验收。被验文件与内容Hash见[输入清单](tool-validation-inputs.json)，包含新增文件，不仅是Git已跟踪Diff。

## 执行与结果

| 验证 | 实际结果 |
|---|---|
| `python -B -m unittest discover -s tools -p 'test_*.py' -v`（新增CLI测试之前） | 20项通过，24.996秒 |
| `python -B -m unittest discover -s tools -p 'test_harness_patch_cli.py' -v` | 新增1项真实Git/CLI集成测试通过，11.092秒 |
| 当前修改与新增Markdown本地链接检查 | 40处、缺失0；不含远程可用性和全部章节锚点 |
| `git diff --check` | 通过；仅既有换行符规范化提示 |

共21项测试分两批执行，不称同一次完整套件运行。先前20项所验证的正式工具代码在本轮未修改；本轮仅新增和修正CLI测试夹具。

## 集成测试范围

在临时Git仓库中创建真实下一Patch版本源提交，修改一处无行为变化的规范注释，真实执行Git Diff、范围规划、指定暂存目录装配、完整Patch冻结及两次归档。断言变化来源、共享依赖范围、目标身份、文件数量、ZIP Hash一致，以及旧包Hash不变。不模拟目标源提交或Git Diff，不创建远程Release，不修改当前仓库提交历史。

最初夹具直接克隆本地仓库，被沙箱与原仓库所有权差异拒绝；进程级路径例外未解决，未改全局Git配置。改为本地bundle传输；首次对匿名提交生成bundle被Git拒绝，随后用HEAD生成bundle并检出包绑定的固定源提交，完成测试。这些准备失败保留为方法修正，不计为产品构建失败或首次全程成功。

## 独立性与限制

复用此前对同一正式来源及工具代码的独立审查结论：来源证据失效规则冲突、Envelope范围遗漏、包级入口契约遗漏均已修复并由原Reviewer定向复核关闭。此次不额外调用子Agent，不将新增测试自检称为独立语义审查。

尚未验证新规则经预编译后的Agent实际调用频率、客户端新会话行为或正式发布流程；当前仓库0.13.0包未重建。本次通过说明工具命令链和已覆盖边界可用，不能据此批准把新增规范能力作为Patch发布。后续修改输入清单中的代码或规则后，应判断受影响验证范围。
